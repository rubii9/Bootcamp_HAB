'use strict';

/**
 * `console.log` nos permite mostrar un mensaje por consola.
 * También puede servir para mostrar el contenido de una variable.
 * Su función es meramente informativa, no está devolviendo un
 * dato con el que podamos trabajar.
 *
 * `alert` tiene una función similar a `console.log` pero en lugar
 * de mostrar un mensaje por consola lo muestra en la ventana del
 * navegador.
 *
 */

// alert('Hello world!');

/**
 * Declaración de variables en JS.
 *
 *   - `const` El valor de una constante no puede cambiarse.
 *
 *   - `let` Tiene un funcionamiento parecido a let, con la
 *           diferencia de que su valor sí puede ser alterado.
 *
 * `¡Ojo!` No hay que confundirse con el tema de constantes
 * cuando usamos `arrays` y `objects`, en estos casos sí está per-
 * mitido usar constantes.
 *
 * También es importante dar un nombre adecuado a las variables.
 * Generalmente debería ser un nombre que nos ayude a identificar
 * su contenido. Una variable no debería contener números o
 * símbolos.
 *
 * Para asignar un valor a una variable se emplea el operador
 * `=`.  
 *
 */

/* const name = 'Pedro';

// name = 'David';

let age = 24;

age = 25;

console.log('Mi nombre es ' + name + ' y tengo ' + age + ' años.');

console.log(`Mi nombre es ${name} y tengo ${age} años.`); */

/**
 * También es posible asignar el valor a una variable utilizando
 * el denominado `prompt`. Este método mostrará una ventana en el
 * navegador y solicitará un dato al usuario.
 *
 * `¡Ojo!` El tipo de dato que asigna el prompt es siempre de tipo
 * `string`.
 *
 */

/* let myAge;

myAge = parseInt(prompt('¿Cuál es tu edad?'))

console.log(myAge);

console.log(typeof myAge);  */
